/**
 * ExplorerPanel — file browser for the connected machine's folder tree.
 * Replaces the old Sessions+Connections explorer with a file tree view.
 * Defaults to fully expanded — no collapsed sections.
 */
export { FileBrowser as ExplorerPanel } from './FileBrowser';
